{
  "name": "kips-collection",
  "version": "1.0.0",
  "description": "KIPS Collection frontend with local Node API for products and cart.",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "seed": "node seed.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^7.0.0"
  }
}
